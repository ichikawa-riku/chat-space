.main-massages__massage
  .main-massages__massage--name
    = message.user.name
  .main-massages__massage--date
    = message.created_at.strftime("%Y/%m/%d %H:%M")
  .main-massages__massage--text
    = message.content
  = image_tag message.image.url if message.image.present?
